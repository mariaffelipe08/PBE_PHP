create database Atv_Avaliativa01;
use Atv_Avaliativa01;

CREATE TABLE livro(
	ID integer auto_increment primary key,
    NOME varchar(255),
    EMAIL varchar(255),
    NOMELIVRO varchar (255)
    
);

SELECT * FROM livro;